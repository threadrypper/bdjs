/**
 * Normalizes an instruction name.
 * @param name The instruction name.
 * @returns The normalized instruction name.
 */
export const normalizeInstructionName = (name: string) => name.slice(1).toLowerCase()
